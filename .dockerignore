# Reinstalled / rebuilt inside the image — never copy host artifacts in.
**/node_modules
**/dist
# Runtime state (relocated to TAP_DATA_DIR=/data in the container).
config.json
logs
*.log
# VCS / tooling / editor noise.
.git
.gitignore
.idea
.omc
.sisyphus
.playwright-cli
.DS_Store
# Not needed to build or run the production image.
test
docs
Dockerfile
.dockerignore
